extends layout

block content

  .container
    .row
      -if(essay !== undefined)
          h2.essay-title= essay.title
          h3.essay-date= essay.updatedAt
          pre.essay-format= essay.essay
